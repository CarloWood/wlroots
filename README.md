# wlroots

The master branch is the same as upstream.

The branch x11_backend has the following additional change(s):

* Make X11 backend work with Nvidia drivers
  (upstream refused this fix with the comment "I am not
  going to add Nvidia specific hacks to wlroots")
